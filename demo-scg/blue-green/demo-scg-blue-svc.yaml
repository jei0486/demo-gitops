apiVersion: v1
kind: Service
metadata:
  name: demo-scg-blue-svc
  namespace: gateway
  labels:
    app: demo-scg
spec:
  ports:
  - protocol: TCP
    name: http
    port: 9090
    targetPort: http
  selector:
    app: demo-scg